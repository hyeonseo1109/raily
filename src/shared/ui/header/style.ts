import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    width: "100%",
    height: 50,
    position: "relative",
    paddingHorizontal: 20,
    justifyContent: "center",
    // alignItems: "center",
    backgroundColor: "#afa281",
  },

  title: {
    fontSize: 25,
    fontWeight: 900,
    color: "#fff",
  },
});
